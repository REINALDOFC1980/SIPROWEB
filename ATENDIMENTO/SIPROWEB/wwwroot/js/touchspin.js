 class TouchSpin {
    //constructor(inputId) {
    //    this.inputElement = document.getElementById(inputId);
    //    this.initialize();
    //}

    //initialize() {
    //    const container = this.inputElement.closest('.input-group');
    //    const incrementButton = container.querySelector('.bootstrap-touchspin-up');
    //    const decrementButton = container.querySelector('.bootstrap-touchspin-down');

    //    incrementButton.addEventListener('click', () => this.increment());
    //    decrementButton.addEventListener('click', () => this.decrement());
    //}

    //increment() {
    //    let currentValue = parseInt(this.inputElement.value) || 0;
    //    this.inputElement.value = currentValue + 1;
    //}

    //decrement() {
    //    let currentValue = parseInt(this.inputElement.value) || 0;
    //    if (currentValue > 0) {
    //        this.inputElement.value = currentValue - 1;
    //    }
    //}
}
